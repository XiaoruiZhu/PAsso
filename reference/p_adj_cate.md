# p_adj_cate

p_adj_cate

## Usage

``` r
p_adj_cate(Z)
```

## Arguments

- Z:

  A numerical vector that inputs the latent variable for generating
  probabilities of adjacent categories regression model.

## Value

A matrix (n by level of respones plus 1) of probabilities of the
adjacent categories model.
